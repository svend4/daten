# Nixpacks configuration for Railway
# Указываем как собирать приложение

[phases.setup]
nixPkgs = ["python311", "postgresql"]

[phases.install]
cmds = [
  "pip install -r requirements.txt"
]

[phases.build]
cmds = [
  "echo 'Build complete'"
]

[start]
cmd = "uvicorn информационная-ОС.ios_bootstrap.main:app --host 0.0.0.0 --port $PORT"
